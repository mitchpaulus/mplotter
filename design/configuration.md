# Configuration

We are going to have simple configuration for our plotter and data sources.

Many database sources, such as Influx can be centralized to a single JSON configuration/database format.
It should be at `%LOCALAPPDATA%/mplotter/config.json`. An example JSON is provided at the root of the repository.

This will provide the location for tagging and organizing trends.
